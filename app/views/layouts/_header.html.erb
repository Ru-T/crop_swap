<div class="container">
<header class="navbar-fixed-top">
    
        <a href="#"class="img navbar-brand"><%= image_tag asset_url ("cropswap-logo.png") %></a>
        <nav class="navbar-default navbar-fixed-top">
            <ul>
              <button type="button" class="btn btn-default navbar-btn">About</button>
              <% if session[:user_id]%>
                <li><%= link_to 'Logout', session_destroy_path, method: :delete%></li>
              <% else %>
                <li> <%= link_to "Log in", session_new_path %> </li>
              <% end %>
            </ul>
        </nav>

</header>
</div>
